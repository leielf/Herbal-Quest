package com.example.elixirapp;

import javafx.scene.image.Image;
import javafx.scene.image.ImageView;

public class Block extends GameObject{

    public Block(Image img, double x, double y) {
        super(img, x, y);
    }
}
